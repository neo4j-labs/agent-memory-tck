[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    unit: pure-unit tests — no I/O
    integration: integration tests with mocked HTTP transport
    e2e: end-to-end tests against the live hosted service (skipped if MEMORY_API_KEY unset)

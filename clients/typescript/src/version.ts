/**
 * Package version constant. Kept in sync with package.json via the
 * `tsup` build and a CI assertion. Imported by the transports to build
 * the default User-Agent header.
 */
export const VERSION = "0.1.0";
